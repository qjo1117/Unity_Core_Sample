using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager
{
    // TO DO : 클라에서 필요한 상호작용할 매니저들을 정의 후 Update를 해준다.
    // TO DO :  각 매니저는 자신들이 관리할 객체들을 List형태로 가지고 있는다.
    //          매니저들은 객체를 메모리적으로 맵핑(자료구조, 변수로 자신이 정보를 알고 있는 상태)과 함께 상호작용을 할 함수를 정의할 예정임.
    

    // TO DO : 각 객체는 정보로 부터 전달받은 것을 토대로 stat를 조절해준다.

    // 참고로 이놈 아이콘이 톱니바퀴인데 유니티 내부에서 GameManager로 이름지으면 톱니바퀴가 됨 (신경 안써도됨)
    void Start()
    {

    }

    void Update()
    {
        
    }
}
